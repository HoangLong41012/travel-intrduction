import { InputLabel } from '@mui/material';
import { styled } from '@mui/material/styles';

const HInputLabel = styled(InputLabel)({
  color: '#000',
  fontSize: '0.75rem',
  lineHeight: '1rem',
});

export default HInputLabel;
